name: Update gist
on:
  repository_dispatch:
    types: [build-event]
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: a26654e1e0881c8b8971f728df814e48
          TIMEZONE: Asia/Seoul
